const Sales = require("../../sales-app/src/models/sales")

const getWeek = ()=>{
    // const date = new Date(createdAt).getTime()
    const diff = Date.now()-604800000
    
    return 
}

const answer=getWeek()
console.log(answer)
// const newDate=new Date(answer)
// console.log(newDate)